# Core dependencies
numpy>=1.24.0
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0
accelerate>=0.20.0

# OpenAI-compatible API client (for VLM inference)
openai>=1.0.0

# Image quality metrics
scikit-image>=0.21.0
lpips>=0.1.4

# Qwen-Agent tool registry + Assistant loop (tool execution)
# NOTE: we only use qwen_agent.tools.base / qwen_agent.agents. The heavy
# `[gui]` extra is NOT required — convert_fncall_to_text is ported locally
# in rededit/fncall.py to avoid pulling in gradio.
qwen-agent>=0.0.9
soundfile>=0.12.0

# Additional utilities
pandas>=1.5.0
requests>=2.28.0
tqdm>=4.65.0
python-dotenv>=1.0.0
python-dateutil>=2.8.0

# Classifiers (CLIP-based)
open_clip_torch>=2.0.0

# UnsafeBench dataset loader (rededit/unsafe_datasets.py)
datasets>=2.14.0
scikit-learn>=1.0.0

# Optional: for some classifiers
# onnxruntime>=1.15.0
# einops>=0.7.0
# timm>=0.9.0
